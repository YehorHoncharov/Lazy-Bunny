model Comment {
  id        Int      @id @default(autoincrement())
  date      DateTime @default(now())
  author    String
  text      String
  movies Movie @relation(fields: [commentId], references: [id])
  commentId Int
}