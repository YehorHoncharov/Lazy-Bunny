model Movie {
    id       Int     @id @default(autoincrement())
    name String
    rating Int
    year Int
    language String
    country String
    ageRating Int
    actors MovieToActor[]
    comments Comment[]

    Genre Relation[]
}